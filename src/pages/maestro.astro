---
// /maestro is a legacy URL from when the playlist surface had its own route.
// Everything role-gated now lives at /admin; the AdminPage component filters
// tabs by role so maestro-only users land on the playlist tab automatically.
// Permanent redirect preserves any outstanding bookmarks.
return Astro.redirect('/admin', 301);
---
