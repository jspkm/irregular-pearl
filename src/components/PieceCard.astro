---
interface Props {
  id: string;
  title: string;
  composer_name: string;
  instruments: string[];
  era: string;
  difficulty: string;
  duration_minutes: number | null;
}

const { id, title, composer_name, instruments, era, difficulty, duration_minutes } = Astro.props;
---
<a
  href={`/piece/${id}`}
  class="block p-4 bg-surface border border-border rounded-lg hover:border-muted hover:shadow-sm transition-all"
>
  <h3 class="font-semibold text-sm leading-tight mb-1 text-ink">{title}</h3>
  <p class="text-xs text-muted mb-2">{composer_name}</p>
  <div class="flex flex-wrap gap-1.5">
    {instruments.map((inst) => (
      <span class="text-[11px] px-1.5 py-0.5 bg-bg rounded text-muted">{inst}</span>
    ))}
    <span class="text-[11px] px-1.5 py-0.5 bg-bg rounded text-muted">{era}</span>
    {duration_minutes && (
      <span class="text-[11px] text-muted">~{duration_minutes} min</span>
    )}
  </div>
</a>
